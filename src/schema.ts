export type FormInputs = {
    name: string,
    symbol: string,
    description: string,
    nftStorageApiKey: string,
    mainImage: FileList,
    imageDirectory: FileList,
    metadataDirectory: FileList,
  };